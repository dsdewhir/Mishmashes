<h1>Listing spams</h1>

<table>
  <tr>
    <th>Spam</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @spams.each do |spam| %>
  <tr>
    <td><%= spam.spam %></td>
    <td><%= link_to 'Show', spam %></td>
    <td><%= link_to 'Edit', edit_spam_path(spam) %></td>
    <td><%= link_to 'Destroy', spam, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Spam', new_spam_path %>
